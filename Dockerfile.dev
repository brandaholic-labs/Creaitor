# Development Dockerfile for Next.js application
# Base image: Node.js 20 Alpine (lightweight)
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files for dependency installation
COPY package.json package-lock.json ./

# Install dependencies (including dev dependencies for hot reload)
RUN npm install

# Copy source code (will be overridden by volume mount in docker-compose.yml for hot reload)
COPY . .

# Expose Next.js development server port
EXPOSE 3000

# Default command: start Next.js development server
CMD ["npm", "run", "dev"]

